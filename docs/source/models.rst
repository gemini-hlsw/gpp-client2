Output models
=============

Every model operations can return, generated from the merged GPP schemas
and rendered straight from the code. There is one model per schema type,
shared by all operations; a field defaults to ``UNSET`` until an operation
selects it (see :doc:`reading`).

.. automodule:: gpp_client2._generated.models
   :members:
   :undoc-members:

