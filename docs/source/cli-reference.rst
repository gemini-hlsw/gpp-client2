CLI reference
=============

The full command tree, generated from the CLI itself. Because commands are
derived from the Python API by reflection, this page always lists every
domain method exactly as the installed version exposes it. Usage patterns
and examples are in :doc:`cli`.

.. typer:: gpp_client.cli.app
   :prog: gpp
   :show-nested:
   :make-sections:
   :width: 76
