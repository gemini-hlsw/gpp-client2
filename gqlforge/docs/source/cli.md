# CLI reference

Rendered from the CLI itself, so it cannot drift from the installed
version. Every command reads `[tool.gqlforge]` from the pyproject.toml
in the current directory.

```{eval-rst}
.. typer:: gqlforge.__main__:app
   :prog: gqlforge
   :show-nested:
   :make-sections:
   :width: 80
```
