# Domain APIs

One page per client attribute. Every domain has an async twin with an
identical surface (`AsyncProgramAPI`, and so on - a conformance test
enforces it), so each is documented once, in its sync form. The
day-to-day patterns shared by all domains are in {doc}`../reading` and
{doc}`../writing`.

```{toctree}
:maxdepth: 1

programs
observations
targets
attachments
calls-for-proposals
goats
scheduler
workflow-state
```
