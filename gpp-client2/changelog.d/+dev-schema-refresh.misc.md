Refresh the committed development schema (previous copy dated to early August): picks up the ToO trigger types and `tooActivationCeiling` fields, archive-duplication search types, and GNIRS imaging/MOS modes. Development restructured `GnirsSpectroscopy` (`centralWavelength`/`initialCentralWavelength` became plural config lists, `coadds` was removed), so those selections now prune on development and return `UNSET` there while production still serves them.
